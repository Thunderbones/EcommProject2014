<%= paginate @items %>
<% if(@items.size < 1) %>
  <p class="item">No Items</p>
<% else %>
  <% @items.each do |item| %>
    <div class="item">
      <%= render item %>
      <span><%= link_to 'Show', item %></span>
    </div>
  <% end %>
<% end %>